# [PROVENANCE_GAP] Stable gfx1151 tarball integrity

The exact AMD URL is frozen, but this capture did not locate an AMD-published detached checksum, detached signature, transparency record, or artifact SBOM for the stable tarball. HTTPS transport and a filename are not a substitute for an authenticated expected digest.

Promotion rule: acquire the bytes, compute SHA-256/SHA-512, retain HTTP headers, and authenticate the expected digest through a separately trusted AMD channel. Until that is done, `promotable: false` remains mandatory.
