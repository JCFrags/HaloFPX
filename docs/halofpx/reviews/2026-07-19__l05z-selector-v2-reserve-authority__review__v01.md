# L05z closed selector-v2 and exact reserve-authority independent review

**Result: ACCEPT_FOR_CLOSED_SELECTOR_V2_AND_EXACT_RESERVE_ALIAS_CORRECTION_NOT_MAPPED_ROLE_EXECUTION_ADMISSION_NOT_L05Z_PROMOTION.**

The reviewed test-only overlay creates 4,446 versioned cases from 247 mapped
roles and 18 exact PRE/LATE errno profiles. Every case remains closed, has no
live matching or run entry point, and binds one-to-one to the existing frozen
compatibility case and its 64-character v1 dedup key. The frozen compatibility
and map-v1 authorities remain unchanged.

The reserve correction closes the prior root-`fstatfs` occurrence alias. Its
fail-closed cursor requires root inspection `fstatfs`, FS_INFO,
GET_SUBVOL_INFO, canonical `/proc/self/fd/<decimal-fd>` `readlink`, and reserve
`fstatfs` in each phase. It maps occurrences 1, 2, and 3 only to FSTATFS roles
003, 011, and 019. Raw descriptors above `INT_MAX` reject before conversion;
PRE requires the suppressed `-ENOSYS` exit; LATE validates the complete
`statfs` result, including `f_spare`, before return replacement. Wrong order,
phase, fd, path, output, or residual route state fails closed.

Both Linux Strix Halo nodes passed the default GNU and explicit ISO C++17
strict builds, all 28 focused diagnostics, the static seam, feature-off, and
L02 controls. The live qualification passed 114/114 fresh-filesystem cells:
all 18 PRE/LATE profiles plus inherited EINTR-ONCE at each of three reserve
phases on both nodes. Every passing cell proves exact-route selection,
controller audit, nonzero child and launcher status, whole-root discard, lock
release, and zero residual controller process, mount, or loop device.

The final evidence manifests verify and both zstd bundles list successfully.
Nimo-1 retains manifest `1ae85483...` and bundle `4477cb18...`; nimo-2 retains
manifest `559dbe57...` and bundle `ee110039...`. The node-specific exact-command
records contain the real source, target, golden-vector, and four-argument
matrix invocation paths. The receipt distinguishes 57 receipt files from 171
JSONL records per node. Discovery failures remain labeled nonacceptance
evidence, and the final cleanup record supersedes the self-matching early
process check.

The canonical Wiki durability and fault-injection requirements agree with the
exact-boundary, disposable-target, fail-closed, retained-evidence treatment.
The change is target-native, default-off test infrastructure: it copies no
donor or GPL llama-ai code, introduces no dependency or P3 obligation, changes
no product linkage, enables no persistent server write, and makes no inference
or performance claim. Rollback is removal of this test-only extent; the known
good services and four immutable reference clones remain unchanged.

The independent adversarial findings—frozen dedup lineage, descriptor range,
complete `statfs` validation, and phase/profile matrix coverage—are closed.
Independent final review found no remaining correctness, freshness, clarity,
provenance, security, rollback, performance-claim, or Wiki blocker.

This acceptance does not admit any of the 247 mapped roles, qualify their
4,446 returned-fault cases, promote L05z, enable persistence, or satisfy any
product-performance gate. Those remain separate, risk-proportionate future
milestones.
