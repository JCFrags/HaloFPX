# L61 independent adversarial review

**Verdict:** accept the host-bound harvesting foundation; terminal
`NOT PROMOTED` for the discriminator.

Independent review reconstructed the per-host manifest/path/hash/interpreter
binding, prelaunch client prefix probe, final two-host no-model fixture,
negative cases, runtime chronology, production snapshots, and cleanup. A
focused reviewer run passed 29 tests. The host-bound foundation is accepted:
both fixture streams were harvested on their owning hosts after exact
PID/InvocationID quiescence, authenticated, and cleaned; one-side-missing and
wrong-host/path cases refused.

The runtime discriminator is not accepted. Both response streams were
authoritatively retained as `missing/source_absent`. The instrumentation covers
`RPC_CMD_HALOFPX_GRAPH_AUTH_EXECUTE`, and neither side emitted an authenticated
execute event. Thus the exact boundary is the armed first-chunk scheduler
failure before that seam; crash, response absence/publication, truncation,
malformation, and client decode remain unclassified. The worker's
144-node/193-tensor ordinary graph belongs chronologically to common warmup and
must not be attributed to authenticated first-chunk execution. The line 2026
message is `RPC_CMD_FREE_BUFFER` teardown after the earlier failure.

The review recommends only a future discriminator at the preceding L40/L44
admission and GRAPH_AUTH_COMPUTE/RECOMPUTE client decisions. It does not
support a semantic correction.

Production remained exact and read-only: nimo-2 PID 1535639/50052/NRestarts0
and nimo-1 PID 2356329/8081/HTTP200/NRestarts0. Eight final unit guards and
root/key/path checks support complete disposable cleanup. Rejected fixture and
dry-run attempts remain preserved as such.
