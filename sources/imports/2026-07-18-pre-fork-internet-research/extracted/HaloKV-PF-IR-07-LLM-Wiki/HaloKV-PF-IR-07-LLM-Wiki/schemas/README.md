# HaloKV PF-IR-07 design schemas

These schemas are implementation design artifacts, not registered standards or
wire-format guarantees. Integer CBOR labels and enum values are local to the
proposed `HKV1` format. Implementations must pin a canonical deterministic
encoding, reject duplicate map keys, reject unknown critical extensions, and
publish cross-language test vectors before interoperability is claimed.
