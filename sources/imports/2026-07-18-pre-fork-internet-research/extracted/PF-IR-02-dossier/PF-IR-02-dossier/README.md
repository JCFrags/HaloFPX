# PF-IR-02 evidence dossier

As-of date: **2026-07-18** (America/Los_Angeles project date)

Open `index.html` for the LLM-Wiki view. The Markdown pages are under `pages/`. Raw source captures and excerpts are under `raw/`. Machine-readable manifests are under `manifests/`.

## Integrity

Run:

```sh
./verify_hashes.sh
```

The archive deliberately excludes restricted USB-IF and PCI-SIG normative files. Their public revision metadata, access terms and unresolved clause-level gaps are preserved instead.

No machine qualification was performed. No claim uses `[MACHINE-TESTED]`.
