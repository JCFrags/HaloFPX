# Examples

The shell examples are intended for a local, authenticated server and use environment variables for endpoint/key selection. `llama_user_id` and Anthropic `metadata.user_id` are cache/scheduling hints in the pinned component, not authentication.

Review route/flag evidence in `../18-API-Examples.md` before using against a different revision.
