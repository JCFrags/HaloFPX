# Further Research Wiki Intake

Status: raw, preserved, and extracted; not automatically promoted.

This intake contains the 12 user-supplied research Wiki archives received on 2026-07-17. The original ZIPs are retained under `source-archive/`; each archive is expanded into a same-named folder under `extracted/` so review can occur without modifying the evidence package.

Canonical promotion route:

```text
source-archive -> extracted research candidate -> review -> canonical HaloFPX Wiki -> implementation decision
```

Imported scripts and commands are reference material and were not executed during intake. See `import-receipt.md` for checksums and extraction verification.
